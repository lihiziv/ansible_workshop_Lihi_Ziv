plugin: amazon.aws.aws_ec2
regions:
  - us-east-1
filters:
  instance-state-name: running
  tag:managed: 'True'
keyed_groups:
  - key: tags.Name
    prefix: name_
  - key: tags.Service
    prefix: service_
hostname:
  - dns
